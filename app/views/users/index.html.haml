%h1 Listing User Accounts

%table
  %tr
    %th Email
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.email
      %td= link_to 'Destroy', user_path(user), :method => :delete, :data => { :confirm => 'Are you sure?' }
