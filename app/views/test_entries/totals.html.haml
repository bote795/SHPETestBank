%h1 Test Bank

%table
  %thead
    %th Course
    %th Total
    - @ExamNames.each do |exam|
      %th= exam.name
  %tbody
  - @classes.each do |key,value|
    %tr
      %td= link_to key.name, detail_test_entries_path(:id => key.id)
      -value.each do |k, v|
        %td= v